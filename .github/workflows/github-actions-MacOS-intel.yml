name: MacOS-intel

on:
  push:
    branches: main
  pull_request:

jobs:
  install-intel-compilers:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-11, macos-12]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v3
      - name: Get spack
        run: git clone --depth 1 https://github.com/spack/spack.git
      - name: Install intel compilers
        run: ./spack/bin/spack install --no-cache intel-oneapi-compilers